<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %> | Clearwater Network</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body>
  <!-- NAVBAR -->
  <nav class="navbar">
    <div class="nav-left">
      <a href="/" class="nav-logo">
  <img src="/images/farm-icon.png" class="nav-logo-icon">
  Clearwater Network
</a>

      <% if (hasCAD) { %>
        <a href="/cad" class="nav-link">CAD System</a>
      <% } %>

      <% if (isStaff) { %>
        <a href="/staff" class="nav-link">Staff Dashboard</a>
      <% } %>
    </div>

    <div class="nav-right">
  <% if (!user) { %>
    <a href="/auth/discord" class="discord-login-btn">
  <img src="https://cdn-icons-png.flaticon.com/512/2111/2111370.png" alt="Discord Logo">
  <span>Login with Discord</span>
</a>
  <% } else { %>
    <div class="nav-user">
      <img
        src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png"
        class="nav-avatar"
        onerror="this.src='/default-avatar.png'"
      />
      <span class="nav-username"><%= user.username %></span>
      <a href="/logout" class="btn secondary nav-btn">Logout</a>
    </div>
  <% } %>
</div>
  </nav>

  <!-- PAGE CONTENT -->
  <main class="page-content">
    <%- body %>
  </main>
</body>
</html>