<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %> | Clearwater Network</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body>
  <!-- NAVBAR -->
  <nav class="navbar">
    <div class="nav-left">
      <a href="/" class="nav-logo">Clearwater Network</a>
      <a href="/cad" class="nav-link">CAD</a>

      <% if (isStaff) { %>
        <a href="/staff" class="nav-link">Staff Dashboard</a>
      <% } %>
    </div>

    <div class="nav-right">
      <% if (!user) { %>
        <a href="/auth/discord" class="discord-login-btn">
          <img src="https://cdn.discordapp.com/icons/81384788765712384/a_4b8e6f8e8e8e8e8e8e8e8e8e8e8e8e8e.gif?size=64" alt="Discord Logo">
          <span>Login with Discord</span>
        </a>
      <% } else { %>
        <div class="nav-user">
          <img
            src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png"
            class="nav-avatar"
            onerror="this.src='/default-avatar.png'"
          />
          <span class="nav-username"><%= user.username %></span>
          <a href="/logout" class="btn secondary nav-btn">Logout</a>
        </div>
      <% } %>
    </div>
  </nav>

  <!-- PAGE CONTENT -->
  <main class="page-content">
    <%- body %>
  </main>
</body>
</html>